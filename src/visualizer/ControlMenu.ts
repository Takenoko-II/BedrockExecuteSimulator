import { Player } from "@minecraft/server";
import { ActionFormWrapper } from "@ui-wrapper";
/*
export class ControlMenu {
    public main(player: Player) {
        const that = this;

        return new ActionFormWrapper()
            .title("Bedrock Execute Simulator")
            .button({
                name: "コマンドの作成"
            })
            .button({
                name: "コマンドを選択",
                on(player) {
                    that.selectCommand(player);
                }
            })
            .open(player);
    }

    public selectCommand(player: Player) {
        const that = this;

        return new ActionFormWrapper()

    }
}
*/